#!/usr/bin/env amyplan

@function $main($argv,$env)
  @locvar $i = 0
  @locvar $key = 0
  @locvar $val = 0
  @for($i = 0, $i < $argv[], $i = $i + 1)
    @dump($argv[$i])
  @endfor
  @fordict $key, $val ($env)
    @dump($key . "=" . $val)
  @endfor
  @return 0
@endfunction
